handleInputChange = event => {
  this.setState({
    userNameInput: event.target.value,
  });
  button inputValue.length >= 3 ? isDisabled ? 'disabled' : ''

  this.userNameInput.reset();
};


<button className="btn" disabled={isDisabled}>


function Input({ type, label, name, onChange }) {
